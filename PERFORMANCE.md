# 页面加载性能优化

本轮针对 HTML 资源发现、首屏 JavaScript 和重复网络请求做了以下修改：

- HTML head 加入带版本哈希的 `modulepreload`，让浏览器在解析长文章正文之前发现主脚本；主样式保留现有异步加载方式。
- 文章差异计算库改为动态加载。首次阅读和内容未变化时不下载该库；需要标记文章更新时才加载。
- 预取和点击共享进行中的页面请求，避免同时下载、解析同一篇文章；成功页面使用最多 12 项的最近使用缓存，失败请求可重试。
- 搜索索引共享进行中的下载；较早的搜索不能覆盖较新的输入结果，下载失败后可重试。
- 图库后台预载遵守现有省流量、蜂窝和慢速网络判断，用户点击图片仍可正常加载图库。

## 测量与验证

使用相同 esbuild 配置（bundle、ESM splitting、minify），`write:false` 在内存中构建，并递归统计入口的静态依赖。基线为 `437f27f` 的 `theme/app.js`。

| 首次加载 JavaScript | 优化前 | 优化后 | 减少 |
| --- | ---: | ---: | ---: |
| 未压缩 | 29,670 B | 27,293 B | 8.0% |
| gzip | 10,739 B | 9,776 B | 9.0% |

两次入口均无额外静态依赖输出；动态差异计算库未计入首屏传输量，需要使用该功能时仍会下载。此结果是资源体积对比，不是实测 LCP 或页面加载时间提升。

新增请求缓存测试覆盖并发请求合并、失败重试和最近使用淘汰；相关测试共 14 项通过。`npm run lint` 和新增模块语法检查通过，esbuild 打包成功。

当前环境缺少 WeasyPrint，完整站点构建尚未验证；没有浏览器性能跟踪或 Lighthouse 测量。部署前应在完整构建环境执行 `npm test`，并检查直接打开长文章、搜索、预取后点击、SPA 返回、文章更新标记及图库交互。
