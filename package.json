{
  "name": "freshmark-static-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test tests/static-build.test.mjs",
    "lint": "node --check scripts/build.mjs && node --check scripts/dev.mjs && node --check theme/app.js"
  },
  "dependencies": {
    "katex": "^0.17.0"
  }
}
