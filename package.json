{
  "name": "freshmark-static-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "new": "node scripts/new.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "lint": "node --check scripts/build.mjs && node --check scripts/dev.mjs && node --check scripts/new.mjs && node --check lib/markdown.mjs && node --check lib/templates.mjs && node --check theme/app.js"
  },
  "dependencies": {
    "katex": "^0.17.0",
    "markdown-it": "^14.3.0"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "esbuild": "^0.28.1",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.49.0"
  }
}
