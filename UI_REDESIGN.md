# Freshmark · 纸上实验室

本轮把界面重设计为可探索的理科杂志：纸白与墨黑排版、朱红标记、共焦椭圆线稿、编号文章索引，以及适合长文与公式的阅读页面。提供完整浅色/深色、中英文与移动端布局。

## 实现

- 首页保留原有精选规则；完整索引包含所有文章，编号及总数来自实际发布内容。显式分类优先，缺失时按内容目录推导展示标签，不修改文章 frontmatter 或搜索 API 数据格式。
- `theme/editorial.css` 同时进入关键 CSS 和延后加载的样式包，保持首屏布局一致。沿用现有字体、按需脚本、SVG 和响应式图片，没有增加 UI 框架或外部字体依赖。
- 文章页加入学科眉题，桌面正文 18px、移动端 17px，行高 1.8；保留公式、图片、下载、分享、目录和评论行为。PDF 样式未修改。
- 搜索弹层移到页面容器外，背景使用 inert；支持焦点循环、Esc 关闭和焦点恢复。移动端使用全屏搜索，桌面使用居中面板。
- 关于页、404、评论及登录表单沿用共同视觉规则；焦点、触控目标和减少动态效果设置得到相应处理。

## 验证

- 在项目忽略目录 `.freshmark-cache/venv` 内安装 WeasyPrint 70，首次生成 80 篇 PDF；随后完整重建复用全部 80 篇缓存。
- `FRESHMARK_WEASYPRINT=/data/data/com.termux/files/home/freshmark/.freshmark-cache/venv/bin/weasyprint npm test`：83 项测试全部通过。
- 语法检查及性能预算测试通过。
- Chromium 149 浏览器检查：360、390、768、1440px，中文/英文、首页/文章、浅色/深色，共 32 组，无页面级横向溢出。
- 搜索焦点进入/循环/恢复、无结果搜索、分类筛选、SPA 导航、移动端目录、关于页/404，以及关闭 JavaScript 后的基本内容展示检查通过。
- 人工查看了桌面与手机、浅深色和中英文截图。正文与纸色对比度约 14:1；辅助文字及强调文字组合最低约 4.92:1。
- 本地浏览器检查使用默认静态构建，评论功能未启用；真实账户、发信及评论提交链路未在浏览器中执行。其现有单元测试通过。

截图与机器结果位于 `.freshmark-cache/ui/`，其中 `before-mobile.png`、`after-mobile.png` 为同环境改版前后对比；`zh-home-1440-light.png` 等为最终设计截图。它们是本地测试产物，不纳入 Git。

## 性能记录

比较基线为 `4217b72`，使用相同构建依赖和 gzip 压缩。以下数字针对实际生成的中文首页：

| 资源 | 改版前 | 改版后 |
| --- | ---: | ---: |
| 首屏 JS gzip | 9,795 B | 9,921 B |
| 内联关键 CSS gzip | 2,224 B | 4,027 B |
| 完整首页 HTML gzip | 15,644 B | 18,496 B |

增加的 HTML/CSS 承载新布局、几何图形和完整索引；首屏 JS 仅增加约 1.3%，关键 CSS 仍低于 5KB。

另使用本机 gzip HTTP 服务、390×844 视口、Chromium 149、4 倍 CPU 降速、配置 150ms 延迟及约 1.6Mbps 下载限速，每版交替执行三次清缓存访问。FCP/LCP 中位数：旧版 404ms，新版 420ms；CLS 中位数：旧版约 0.00015，新版 0。原始数据保存在 `.freshmark-cache/ui/performance.json`。

这些是同机实验值，样本少且存在调度波动，不表示线上加载速度提高；最终仍需在真实 VPS、网络和设备上观察。未推送、部署或修改生产服务。
